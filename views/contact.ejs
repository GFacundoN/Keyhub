<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | KeyHub</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="min-h-full flex flex-col bg-gray-50">
  <%- include('partials/navbar') %>

  <div class="flex-1 py-16 px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl mx-auto">
      <div class="text-center mb-12">
        <div class="inline-flex items-center justify-center w-24 h-24 bg-gradient-to-br from-primary-700 to-primary-800 rounded-full mb-6 shadow-xl">
          <svg class="w-14 h-14 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
        </div>
        <h1 class="section-header text-center"><%= title %></h1>
        <p class="text-xl text-navy-600">¿Tienes alguna pregunta? Estamos aquí para ayudarte</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
        <div class="card text-center p-6">
          <div class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-accent-cyan to-accent-teal rounded-full mb-4 shadow-lg mx-auto">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
            </svg>
          </div>
          <h5 class="text-lg font-bold text-navy-900 mb-2">Teléfono</h5>
          <p class="text-navy-600">+54 11 1234-5678</p>
        </div>
        <div class="card text-center p-6">
          <div class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-primary-700 to-primary-800 rounded-full mb-4 shadow-lg mx-auto">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
          </div>
          <h5 class="text-lg font-bold text-navy-900 mb-2">Email</h5>
          <p class="text-navy-600">info@keyhub.com</p>
        </div>
        <div class="card text-center p-6">
          <div class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-accent-emerald to-accent-teal rounded-full mb-4 shadow-lg mx-auto">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
          </div>
          <h5 class="text-lg font-bold text-navy-900 mb-2">Ubicación</h5>
          <p class="text-navy-600">Buenos Aires, Argentina</p>
        </div>
      </div>

      <div class="card p-8">
        <h3 class="section-subheader">Envíanos un mensaje</h3>
        <form id="contactForm" action="/contact" method="POST" class="space-y-6">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label for="nombre" class="block text-sm font-bold text-navy-800 mb-2">Nombre</label>
              <input type="text" class="input-field" id="nombre" name="nombre" required>
            </div>
            <div>
              <label for="email" class="block text-sm font-bold text-navy-800 mb-2">Email</label>
              <input type="email" class="input-field" id="email" name="email" required>
            </div>
          </div>
          <div>
            <label for="asunto" class="block text-sm font-bold text-navy-800 mb-2">Asunto</label>
            <input type="text" class="input-field" id="asunto" name="asunto" required>
          </div>
          <div>
            <label for="mensaje" class="block text-sm font-bold text-navy-800 mb-2">Mensaje</label>
            <textarea class="input-field" id="mensaje" name="mensaje" rows="5" required></textarea>
          </div>
          <button type="submit" id="submitBtn" class="btn-primary text-lg">
            <span id="btnText">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
              </svg>
              Enviar Mensaje
            </span>
            <span id="btnLoader" class="hidden">
              <svg class="animate-spin h-5 w-5" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
              </svg>
              Enviando...
            </span>
            <span id="btnSuccess" class="hidden">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              ¡Enviado!
            </span>
          </button>
        </form>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
  <script>
    document.getElementById('contactForm').addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const submitBtn = document.getElementById('submitBtn');
      const btnText = document.getElementById('btnText');
      const btnLoader = document.getElementById('btnLoader');
      const btnSuccess = document.getElementById('btnSuccess');
      
      // Deshabilitar botón y mostrar animación de carga
      submitBtn.disabled = true;
      btnText.classList.add('hidden');
      btnLoader.classList.remove('hidden');
      
      // Recopilar datos del formulario y convertir a URLSearchParams
      const formData = new FormData(this);
      const urlEncodedData = new URLSearchParams(formData);
      
      try {
        // Enviar formulario
        const response = await fetch('/contact', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
          },
          body: urlEncodedData
        });
        
        if (response.ok) {
          // Mostrar check verde
          btnLoader.classList.add('hidden');
          btnSuccess.classList.remove('hidden');
          submitBtn.classList.remove('bg-primary-700');
          submitBtn.classList.add('bg-accent-emerald');
          
          // Esperar un momento y redirigir
          setTimeout(() => {
            window.location.href = response.url;
          }, 1500);
        } else {
          // Error: restaurar botón
          btnLoader.classList.add('hidden');
          btnText.classList.remove('hidden');
          submitBtn.disabled = false;
          alert('Hubo un error al enviar el mensaje. Por favor, intenta nuevamente.');
        }
      } catch (error) {
        // Error de red: restaurar botón
        btnLoader.classList.add('hidden');
        btnText.classList.remove('hidden');
        submitBtn.disabled = false;
        alert('Error de conexión. Por favor, verifica tu conexión a internet e intenta nuevamente.');
      }
    });
  </script>
</body>
</html>
