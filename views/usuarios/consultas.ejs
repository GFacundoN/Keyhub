<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | KeyHub</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .consulta-card {
      border: none;
      border-radius: 16px;
      transition: all 0.3s ease;
      border-left: 4px solid #dee2e6;
    }
    .consulta-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px rgba(0,0,0,0.1) !important;
    }
    .consulta-card.pendiente {
      border-left-color: #ff9800;
      background: linear-gradient(to right, #fff3e0 0%, #ffffff 15%);
    }
    .consulta-card.en-proceso {
      border-left-color: #2196f3;
      background: linear-gradient(to right, #e3f2fd 0%, #ffffff 15%);
    }
    .consulta-card.respondida {
      border-left-color: #4caf50;
      background: linear-gradient(to right, #e8f5e9 0%, #ffffff 15%);
    }
    .consulta-card.cerrada {
      border-left-color: #607d8b;
      background: linear-gradient(to right, #eceff1 0%, #ffffff 15%);
    }
    .status-badge {
      padding: 8px 16px;
      border-radius: 20px;
      font-weight: 500;
      font-size: 0.875rem;
    }
    .header-section {
      background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
      color: white;
      padding: 3rem 0;
      margin-bottom: 2rem;
      border-radius: 0 0 30px 30px;
    }
    .empty-state {
      padding: 4rem 2rem;
    }
    .empty-icon {
      width: 120px;
      height: 120px;
      background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 2rem;
    }
  </style>
</head>
<body class="bg-light">
  <%- include('../partials/navbar') %>

  <div class="header-section">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-md-8">
          <h1 class="display-4 fw-bold mb-2">
            <i class="bi bi-chat-dots-fill me-3"></i>Mis Consultas
          </h1>
          <p class="lead mb-0 opacity-90">Seguimiento completo de todas tus consultas</p>
        </div>
        <div class="col-md-4 text-md-end">
          <a href="/inmuebles" class="btn btn-light btn-lg">
            <i class="bi bi-house-fill me-2"></i>Ver Propiedades
          </a>
        </div>
      </div>
    </div>
  </div>

  <div class="container pb-5">

    <% if (consultas && consultas.length > 0) { %>
      <div class="row g-4">
        <% consultas.forEach(consulta => { 
          let cardClass = 'consulta-card shadow-sm';
          let badgeClass = 'status-badge';
          let icon = '';
          
          if (consulta.estado === 'PENDIENTE') {
            cardClass += ' pendiente';
            badgeClass += ' text-white';
            badgeClass += ' ' + 'bg-warning';
            icon = 'bi-clock-fill';
          } else if (consulta.estado === 'EN_PROCESO') {
            cardClass += ' en-proceso';
            badgeClass += ' text-white';
            badgeClass += ' ' + 'bg-primary';
            icon = 'bi-hourglass-split';
          } else if (consulta.estado === 'RESPONDIDA') {
            cardClass += ' respondida';
            badgeClass += ' text-white';
            badgeClass += ' ' + 'bg-success';
            icon = 'bi-check-circle-fill';
          } else if (consulta.estado === 'CERRADA') {
            cardClass += ' cerrada';
            badgeClass += ' text-white';
            badgeClass += ' ' + 'bg-secondary';
            icon = 'bi-x-circle-fill';
          }
        %>
          <div class="col-12 col-lg-6">
            <div class="card <%= cardClass %> h-100">
              <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-start mb-3">
                  <div class="flex-grow-1 pe-3">
                    <% if (consulta.inmueble_direccion) { %>
                      <h5 class="card-title fw-bold mb-1" style="color: #2d3748;">
                        <i class="bi bi-building text-primary me-2"></i><%= consulta.inmueble_tipo || 'Propiedad' %>
                      </h5>
                      <p class="text-muted mb-2">
                        <i class="bi bi-geo-alt-fill me-1"></i><%= consulta.inmueble_direccion %>
                      </p>
                    <% } else { %>
                      <h5 class="card-title fw-bold mb-2" style="color: #2d3748;">
                        <i class="bi bi-chat-square-text text-primary me-2"></i>Consulta General
                      </h5>
                    <% } %>
                    <% if (consulta.codigo_inmueble) { %>
                      <span class="badge bg-light text-dark border mb-2">
                        <i class="bi bi-hash"></i><%= consulta.codigo_inmueble %>
                      </span>
                    <% } %>
                  </div>
                  
                  <div>
                    <span class="<%= badgeClass %>">
                      <i class="<%= icon %> me-1"></i><%= consulta.estado === 'EN_PROCESO' ? 'En Proceso' : consulta.estado.charAt(0) + consulta.estado.slice(1).toLowerCase() %>
                    </span>
                  </div>
                </div>
                
                <% if (consulta.mensaje) { %>
                  <div class="mb-3 p-3 bg-light rounded">
                    <p class="mb-0 text-dark" style="font-size: 0.95rem;">
                      "<%= consulta.mensaje %>"
                    </p>
                  </div>
                <% } %>

                <div class="d-flex flex-wrap gap-3 text-muted mb-3" style="font-size: 0.875rem;">
                  <div>
                    <i class="bi bi-calendar-event-fill text-primary me-1"></i>
                    <strong><%= new Date(consulta.fecha_consulta).toLocaleDateString('es-AR') %></strong>
                  </div>
                  <div>
                    <i class="bi bi-clock-fill text-primary me-1"></i>
                    <%= new Date(consulta.fecha_consulta).toLocaleTimeString('es-AR', { hour: '2-digit', minute: '2-digit' }) %>
                  </div>
                  <div>
                    <i class="bi bi-tag-fill text-primary me-1"></i>
                    <%= consulta.tipo_consulta || 'INFORMACIÓN' %>
                  </div>
                </div>

                <% if (consulta.inmueble_id) { %>
                  <div class="d-grid">
                    <a href="/inmuebles/<%= consulta.inmueble_id %>" class="btn btn-primary">
                      <i class="bi bi-arrow-right-circle-fill me-2"></i>Ver Propiedad Completa
                    </a>
                  </div>
                <% } %>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="empty-state text-center">
        <div class="empty-icon">
          <i class="bi bi-chat-dots-fill" style="font-size: 3rem; color: white;"></i>
        </div>
        <h3 class="h4 fw-bold mb-3" style="color: #2d3748;">No tienes consultas todavía</h3>
        <p class="text-muted mb-4 mx-auto" style="max-width: 500px;">
          Comienza a explorar nuestras propiedades y realiza tus consultas. 
          Te mantendremos informado sobre el estado de cada una.
        </p>
        <div class="d-flex gap-3 justify-content-center flex-wrap">
          <a href="/inmuebles" class="btn btn-primary btn-lg">
            <i class="bi bi-house-fill me-2"></i>Explorar Propiedades
          </a>
          <a href="/" class="btn btn-outline-secondary btn-lg">
            <i class="bi bi-arrow-left me-2"></i>Volver al Inicio
          </a>
        </div>
      </div>
    <% } %>
  </div>

  <%- include('../partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
