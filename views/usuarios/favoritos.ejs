<!DOCTYPE html>
<html lang="es" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | KeyHub</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="min-h-full flex flex-col bg-gray-50">
  <%- include('../partials/navbar') %>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="mb-6">
      <nav aria-label="breadcrumb" class="mb-4">
        <ol class="flex items-center space-x-2 text-sm text-gray-600">
          <li><a href="/" class="hover:text-primary-600">Inicio</a></li>
          <li class="flex items-center">
            <svg class="w-4 h-4 mx-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
            <a href="/usuarios/<%= user.id %>" class="hover:text-primary-600">Mi Perfil</a>
          </li>
          <li class="flex items-center">
            <svg class="w-4 h-4 mx-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
            <span class="text-gray-900 font-medium">Favoritos</span>
          </li>
        </ol>
      </nav>

      <div class="flex items-center gap-3 mb-4">
        <svg class="w-8 h-8 text-red-500" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
        </svg>
        <div>
          <h1 class="text-4xl font-bold text-gray-900"><%= title %></h1>
          <p class="text-gray-600 mt-1">
            <%= favoritos.length %> <%= favoritos.length === 1 ? 'inmueble guardado' : 'inmuebles guardados' %>
          </p>
        </div>
      </div>
    </div>

    <!-- Lista de favoritos -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% if (favoritos && favoritos.length > 0) { %>
        <% favoritos.forEach(inmueble => { %>
          <div class="card group hover:shadow-2xl transition-shadow duration-300">
            <div class="relative overflow-hidden">
              <img src="https://images.unsplash.com/photo-1560518883-ce09059eeffa?w=400&h=250&fit=crop" class="w-full h-56 object-cover group-hover:scale-110 transition-transform duration-300" alt="<%= inmueble.direccion %>">
              <div class="absolute top-4 right-4">
                <span class="badge badge-info">
                  <%= inmueble.zona_nombre || 'Sin zona' %>
                </span>
              </div>
              <div class="absolute top-4 left-4">
                <button class="favorito-btn bg-white rounded-full p-2 shadow-lg hover:bg-gray-100 transition-colors" 
                        data-inmueble-id="<%= inmueble.id %>"
                        title="Quitar de favoritos">
                  <svg class="w-5 h-5 text-red-500" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                  </svg>
                </button>
              </div>
            </div>
            <div class="p-6">
              <h3 class="text-xl font-semibold text-gray-900 mb-2"><%= inmueble.tipo_nombre || 'Inmueble' %></h3>
              
              <div class="flex items-start gap-2 text-gray-600 mb-3">
                <svg class="w-5 h-5 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                </svg>
                <div>
                  <p class="font-medium"><%= inmueble.direccion %></p>
                  <% if (inmueble.barrio) { %>
                    <p class="text-sm text-gray-500"><%= inmueble.barrio %></p>
                  <% } %>
                </div>
              </div>

              <!-- Características principales -->
              <div class="flex flex-wrap gap-2 mb-4">
                <% if (inmueble.superficie_total_m2) { %>
                  <span class="inline-flex items-center gap-1 px-2 py-1 bg-gray-100 text-gray-700 text-sm rounded">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 8V4m0 0h4M4 4l5 5m11-1V4m0 0h-4m4 0l-5 5M4 16v4m0 0h4m-4 0l5-5m11 5l-5-5m5 5v-4m0 4h-4"></path>
                    </svg>
                    <%= inmueble.superficie_total_m2 %> m²
                  </span>
                <% } %>
                <% if (inmueble.ambientes) { %>
                  <span class="inline-flex items-center gap-1 px-2 py-1 bg-gray-100 text-gray-700 text-sm rounded">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                    </svg>
                    <%= inmueble.ambientes %> amb
                  </span>
                <% } %>
                <% if (inmueble.dormitorios) { %>
                  <span class="inline-flex items-center gap-1 px-2 py-1 bg-gray-100 text-gray-700 text-sm rounded">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>
                    </svg>
                    <%= inmueble.dormitorios %> dorm
                  </span>
                <% } %>
                <% if (inmueble.banos) { %>
                  <span class="inline-flex items-center gap-1 px-2 py-1 bg-gray-100 text-gray-700 text-sm rounded">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                    <%= inmueble.banos %> baños
                  </span>
                <% } %>
              </div>

              <p class="text-gray-600 text-sm mb-4 line-clamp-2 min-h-[40px]">
                <%= inmueble.descripcion ? inmueble.descripcion.substring(0, 100) + '...' : 'Sin descripción' %>
              </p>

              <div class="flex justify-between items-center pt-4 border-t border-gray-200">
                <span class="text-xs text-gray-500">
                  Agregado <%= new Date(inmueble.fecha_agregado).toLocaleDateString('es-AR', { day: 'numeric', month: 'short' }) %>
                </span>
                <a href="/inmuebles/<%= inmueble.id %>" class="inline-flex items-center gap-2 text-primary-600 hover:text-primary-700 font-medium">
                  Ver más
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                  </svg>
                </a>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <div class="col-span-full">
          <div class="bg-blue-50 border-l-4 border-blue-500 p-6 rounded text-center">
            <svg class="w-16 h-16 mx-auto text-blue-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
            </svg>
            <h2 class="text-xl font-semibold text-gray-900 mb-2">No tienes favoritos aún</h2>
            <p class="text-gray-600 mb-4">Empieza a guardar tus inmuebles favoritos para verlos aquí.</p>
            <a href="/inmuebles" class="btn-primary inline-block">
              Explorar Inmuebles
            </a>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <%- include('../partials/footer') %>

  <script>
    // Manejo de favoritos con AJAX
    document.querySelectorAll('.favorito-btn').forEach(btn => {
      btn.addEventListener('click', async function(e) {
        e.preventDefault();
        e.stopPropagation();
        
        const inmuebleId = this.dataset.inmuebleId;
        
        try {
          const response = await fetch('/favoritos/toggle', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({ inmuebleId })
          });
          
          const result = await response.json();
          
          if (result.success) {
            // Remover la tarjeta con animación
            const card = this.closest('.card');
            card.style.opacity = '0';
            card.style.transform = 'scale(0.95)';
            setTimeout(() => {
              card.remove();
              
              // Actualizar contador
              const countElement = document.querySelector('.text-gray-600');
              const cards = document.querySelectorAll('.card').length;
              if (countElement) {
                countElement.textContent = `${cards} ${cards === 1 ? 'inmueble guardado' : 'inmuebles guardados'}`;
              }
              
              // Si no quedan favoritos, mostrar mensaje
              if (cards === 0) {
                location.reload();
              }
            }, 300);
          }
        } catch (error) {
          console.error('Error:', error);
          alert('Hubo un error al modificar favoritos');
        }
      });
    });
  </script>
</body>
</html>
